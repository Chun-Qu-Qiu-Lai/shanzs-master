package com.shanzs.blog.utils;

public class CountS {
  /**
   * 通过采用indexOf + substring + 递归的方式来获取指定字符的数量
   * @param text 指定要搜索的字符串
   */
//  private void index(String text){
//    // 判断字符串中,是否包含指定的字符
//    int indexs = text.indexOf("cs");
//    if(indexs != -1){
//      count++;
//      if(indexs < text.length() - 1){
//        text = text.substring(indexs + 1);
//        index(text);
//      }
//    }


}
